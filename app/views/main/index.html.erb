<%= content_tag "div",
                nil ,
                id: "user-container",
                data: {
                        users: User.select("id, name, email, phone, cellphone, position, location, team_id, admin, comex, compras, director, maintenance")
                } %>
<%= content_tag "div",
                nil ,
                id: "team-container",
                data: {
                        teams: Team.select("id, name, supervisor_id, director_id, cost_center")
                } %>
<%= content_tag "div",
                nil ,
                id: "form_helpers-container",
                data: {
                        form_helpers: FormHelper.all
                } %>
<%= content_tag "div",
                nil ,
                id: "suppliers-container",
                data: {
                        suppliers: Supplier.all
                } %>


